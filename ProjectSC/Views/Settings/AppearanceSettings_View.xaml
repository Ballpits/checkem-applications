<UserControl x:Class="ProjectSC.Views.Settings.AppearanceSettings_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:ProjectSC.Views.Settings"
             xmlns:properties="clr-namespace:ProjectSC.Properties"
             mc:Ignorable="d" d:DesignWidth="1770" d:DesignHeight="940" Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <ResourceDictionary Source="../../Assets/StyleResourceDictionary/ColorConverterDictionary.xaml"/>
    </UserControl.Resources>

    <Grid Background="{Binding Source={x:Static properties:Settings.Default}, Path=LightMainColor, Converter={StaticResource ColorToBrushConverter}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <ScrollViewer x:Name="ThemePickerScrollbar" VerticalScrollBarVisibility="Auto">
        </ScrollViewer>

        <Grid Grid.Column="1">

        </Grid>
    </Grid>
</UserControl>
