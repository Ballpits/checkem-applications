<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:ProjectSC.Views.Settings"
             xmlns:properties="clr-namespace:ProjectSC.Properties"
             xmlns:Theme="clr-namespace:ProjectSC.Views.Theme" x:Class="ProjectSC.Views.Settings.AppearanceSettings_View"
             mc:Ignorable="d" d:DesignWidth="1770" d:DesignHeight="940" Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <ResourceDictionary Source="../../Assets/StyleResourceDictionary/ColorConverterDictionary.xaml"/>
    </UserControl.Resources>

    <Grid Background="{Binding GrayMainColor, Converter={StaticResource ColorToBrushConverter}, Source={x:Static properties:Settings.Default}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="7*"/>
            <RowDefinition Height="87*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="0.3*"/>
        </Grid.ColumnDefinitions>

        <Viewbox Margin="0,10,0,0" Grid.Column="1">
            <TextBlock Text="{Binding ThemeName, Source={x:Static properties:Settings.Default}}" Foreground="{Binding DarkMainColor, Converter={StaticResource ColorToBrushConverter}, Source={x:Static properties:Settings.Default}}" FontFamily="Arial"/>
        </Viewbox>

        <ToggleButton x:Name="DarkModeToggle" IsChecked="{Binding IsDarkModeApplied, Source={x:Static properties:Settings.Default}}" HorizontalAlignment="Center" Grid.Row="1" Grid.Column="1" VerticalAlignment="Bottom" Click="ToggleButton_Click"/>

        <ScrollViewer x:Name="ThemePickerScrollbar" VerticalScrollBarVisibility="Auto" Grid.Column="1" Margin="0.4,70,-0.4,80" Grid.RowSpan="2"/>

        <Theme:ThemePreviewWindow Margin="80" Grid.Row="1"/>
    </Grid>
</UserControl>
