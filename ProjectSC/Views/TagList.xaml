<UserControl x:Class="ProjectSC.Views.TagList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectSC.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="45" d:DesignWidth="940" Height="Auto">

    <Grid Visibility="Visible" Loaded="TagList_Loaded">
        <Button Style="{StaticResource TagButton}" Content="{DynamicResource Dict_CreateTag}" Click="ButtonCreateTag_Click" Margin="0" VerticalAlignment="Center"/>

        <ScrollViewer VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto" Grid.Column="1">
            <StackPanel x:Name="StpTagList" Margin="0" Orientation="Horizontal"/>
        </ScrollViewer>
    </Grid>
    
</UserControl>
