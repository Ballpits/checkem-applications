<UserControl x:Class="Checkem.Windows.CustomComponents.TagBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="45"
             d:DesignWidth="940"
             VerticalAlignment="Stretch"
             HorizontalAlignment="Stretch"
             >

    <Grid Loaded="Grid_Loaded">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button Style="{StaticResource CheckemFlatButton.Round20}" Content="{DynamicResource Dict_CreateTag}" FocusVisualStyle="{DynamicResource CheckemFocusVisual.PrimaryColor}" Click="ButtonCreateTag_Click" Grid.ColumnSpan="1" Height="45" TabIndex="0" Width="100" HorizontalAlignment="Left"/>

        <ScrollViewer VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto" Grid.Column="1">
            <StackPanel x:Name="StpTagList" Margin="0" Orientation="Horizontal"/>
        </ScrollViewer>
    </Grid>
</UserControl>
